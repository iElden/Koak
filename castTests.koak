def test(var1: int var2: double): double {
    local a : double = cast<double>(var1) + var2 + 1.
    local b : int = cast<int>(a) << 2
    cast<double>(b)
}

global x: double = -1.
local t : bool = x > 0. || x < -2.
if (!t) {
    x = test(4 2.)
}
cast<int>(x)
